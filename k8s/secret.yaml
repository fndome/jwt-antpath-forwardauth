apiVersion: v1
kind: Secret
metadata:
  name: jwt-antpath-forwardauth-secret
  namespace: gw
  labels:
    app: jwt-antpath-forwardauth
type: Opaque
stringData:
  # ⚠️ 修改为真实密钥 (与 gaming-partner 的 gpJwtSecret 一致)
  JWT_SECRET_KEY: "CHANGE_ME_AT_LEAST_32_CHARS"
